# Title 
#import os 
#import sys




print('@'*50)
print('\t 성적 통계 프로그램')
print('@'*50)

# cSV 파일 Open
# stream definition 

# open 은 표준 함수 
file = open('Question/Homework/scores.csv','r',encoding='UTF-8') 
#file = open('Question/Homework/scores.csv','r',encoding='ANSI') 

# 아래의 구문은 인코딩 방식이 UTF-8 유니코드인지, ANSI인지에 따라 영향 받음.
# 내 PC에서는 UTF-8일 경우 성공, ANSI에서는 동작 안됨. 

print('file structure :', file)

data=file.read()
#파일 내용을 처음부터 끝까지 전부 읽어버립니다.
#그래서 파일 포인터(cursor)가 이미 파일의 맨 끝(EOF)에 가 있게 됩니다.

print('read_data type :',type(data))   #  <class 'str'> : 읽은 문자가 문자열 타입임 , 처음부터 끝까지 읽었음.
print('read data :',data)


data1=file.readline()
#을 실행하면 이미 끝까지 읽은 상태라서 더 읽을 내용이 없기 때문에:
#(빈 문자열) 이 반환됩니다.
#즉 출력은 대략 이렇게 됩니다:
print('read_data type :',type(data1))   #  <class 'str'> : 읽은 문자가 문자열 타입임 , 처음부터 끝까지 읽었음.
print('read data :',data1)

file.close()
print() # by default , the '\n' is set .



#//////////////////////////////////////////
# 이 코드에서 핵심적으로 궁금해할 만한 부분은 read()와 readline()의 차이입니다.
# 현재 코드 흐름을 보면:
# data=file.read()
# 를 먼저 실행했습니다.
# read()는 파일 내용을 처음부터 끝까지 전부 읽어버립니다.
# 그래서 파일 포인터(cursor)가 이미 파일의 맨 끝(EOF)에 가 있게 됩니다.
# 그 다음:
# data1=file.readline()
# 을 실행하면 이미 끝까지 읽은 상태라서 더 읽을 내용이 없기 때문에:
# ''
# (빈 문자열) 이 반환됩니다.
# 즉 출력은 대략 이렇게 됩니다:
# read data : 전체 파일 내용
# read data :
# 두 번째는 아무것도 안 나옵니다.
# 해결 방법은 3가지 정도 있습니다.

# 1. 파일 포인터를 처음으로 되돌리기 (seek(0))
# data=file.read()
# file.seek(0)
# data1=file.readline()
# print(data1)
# seek(0)은 파일 읽기 위치를 맨 처음으로 이동시킵니다.

# 2. 처음부터 readline()만 사용하기
# line1 = file.readline()
# line2 = file.readline()
# 한 줄씩 읽습니다.

# 3. 가장 많이 쓰는 방식 (with open)
# 파이썬에서는 보통 이렇게 작성합니다.
# with open('Question/Homework/scores.csv', 'r', encoding='UTF-8') as file:
#     data = file.read()
#     print(data)
# 이 방식은 자동으로 파일을 닫아줘서 안전합니다.

# 추가로 코드에 있는 이 부분:
# encoding='UTF-8'
# # encoding='ANSI'
# 는 파일 저장 인코딩과 맞아야 합니다.

# UTF-8로 저장된 파일 → UTF-8
# CP949/ANSI로 저장된 파일 → cp949 또는 euc-kr

# 보통 한국 Windows 환경에서는:
# encoding='cp949'
# 를 많이 사용합니다.

# 정리하면:
# read() = 전체 읽기
# readline() = 한 줄 읽기
# read() 후에는 파일 끝에 도달하므로 readline() 결과가 빈 문자열이 됨
# 다시 읽으려면 seek(0) 필요
# 입니다.
#------------------------------------------------------------------------------

#-------------------------------------------------------------------------------------
# PYTHON 과제 
# 문제. 학생들의 성적이 저장된 scores.csv 파일을 이용한 성적 통계 프로그램을 
# 만들어보시오. 
# 과제 요구사항 
# 1. CSV 파일 읽기 : open() 함수를 사용하여 scores.csv 파일을 읽으시오. 
# [* 첫줄(헤더)을 제외한 나머지 줄의 점수를 숫자(int) 데이터로 변환해야만 
# 계산가능] 
# 2. 데이터 처리 : 각 과목(국어, 영어, 수학)에 대해 다음을 계산하시오. 
# 평균 점수 
# 최고 점수 
# 최저 점수 
# 3. 결과 출력 : 각 과목별 통계 결과를 콘솔에 보기 좋게 출력하시오. 
# 예) 
# [과목별 통계] 
# 국어 - 평균: 85.5, 최고점: 92, 최저점: 76 
# 영어 - 평균: 88.0, 최고점: 94, 최저점: 80 
# 수학 - 평균: 84.0, 최고점: 95, 최저점: 72 
# 4. 결과 파일로 저장 (선택 과제) 
# 각 학생의 총점과 평균도 계산하시오. 
# 각 학생의 이름, 총점, 평균을 포함한 새로운 CSV 파일(result.csv)로 저장하시오. 
# #solution guide 
# 평균, 최고, 최저 점수를 계산하는 것을 반복문과 연산자로 수행하는 문제이지만,(필수 아님) 
# 파이썬의 표준내장함수 중에 이 값들을 쉽게 계산해주는 기능함수가 이미 존재함.  
# 배열(리스트,튜플,딕셔너리)수업에 일부 소개되었음. 즉, 계산을 편히 하려면 읽어들인 값들을 
# 리스트로 만들면 표준함수로 문제를 해결할 수도 있음. 학습하지 않은 내용은 검색하여 수행 
# [표준함수 ex : print(), input(), len(), max(). . . ] 
# 제출해야 할 내용 
# 아래 2가지를 반드시 제출하세요 
# ① Python 소스 코드 (.py) 
# ● 하나의 파일로 제출하거나 
# ● 모듈 형태로 작성된 경우 여러 파일이 있어도 무방함. 
# ② 실행 화면 캡처 (jpg/png + csv ) 
# 다음 항목들을 준비하여 파이썬 파일과 함께 제출 합니다 
# ● 결과 출력 화면 캡처 이미지 
# ● (동작 안될경우) 실행했을때 보여지는 에러 메세지 캡처 이미지 
# ● (선택과제)저장된 파일 이름이 보이는 목록 화면(윈도우 탐색기 or VSCode Explorer) 
# 캡처 
# ● (선택과제)저장된 CSV 파일 
# 제출 방법 
# ● 교강사 공유폴더 > 교과목별 평가 > 01 PYTHON > 제출폴더 > 본인이름폴더 
#------------------------------------------------------------------------------------------